import { NavBareDirective } from './nav-bare-directive';

describe('NavBareDirective', () => {
  it('should create an instance', () => {
    const directive = new NavBareDirective();
    expect(directive).toBeTruthy();
  });
});
