import { NavSelecl } from './nav-selecl';

describe('NavSelecl', () => {
  it('should create an instance', () => {
    const directive = new NavSelecl();
    expect(directive).toBeTruthy();
  });
});
